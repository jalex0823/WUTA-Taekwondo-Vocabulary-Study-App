{
  "name": "wuta-mobile-wrapper",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor wrapper for WUTA Taekwondo Vocabulary Trainer (loads the production web app).",
  "type": "module",
  "scripts": {
    "cap:add:ios": "npx cap add ios",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open:ios": "npx cap open ios",
    "cap:open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/android": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "typescript": "^5.7.2"
  }
}
